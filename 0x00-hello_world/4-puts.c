#include <stdio.h>
/**
 *  main -  A program that prints a line with puts function
 *  Return : 0 (Success)
 */
int main(viod)
{
pust("\"Progtamming is like building a multiligual puzz le ");
return (0);
}
